import { Meta, StoryObj } from "@storybook/react";
import { GmGrid } from "./GmGrid";

const meta: Meta<typeof GmGrid> = {
  component: GmGrid,
}

export default meta;

type Story = StoryObj<typeof GmGrid>

export const Primary: Story = {

}